{% if not isPlaylist  and not isSong and not isAlbum and not isFavoris  %}
    <a  href="{{ path('app_homepage_player',{id: prev.id})  }}"
{% endif %}


{% if playlist is defined %}

    <a  href="{{ path('app_playlist_player',{'playlistId': playlist.id, 'songId': prev.id}) }}" >
{% endif %}

    {% if isSong %}
    <a href="{{ path('app_song_player',{id: prev.id}) }}" >
        {% endif %}

        {% if isAlbum %}
        <a  href="{{ path('app_album_player',{'albumId': album.id, 'songId': prev.id}) }}" >
            {% endif %}

            {% if isFavoris %}
            <a href="{{ path('app_song_favoris_player',{'userId': user.id, 'songId': prev.id}) }}">
{% endif%}