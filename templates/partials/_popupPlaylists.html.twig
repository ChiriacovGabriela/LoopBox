<div class="modal fade" id="exampleModalLong{{ song.id }}" tabindex="-1" role="dialog"
     aria-labelledby="exampleModalLongTitle" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content popup_homepage2">
            <div class="modal-header popup_homepage">
                <h5 class="modal-title" id="exampleModalLongTitle">{% trans %}CHOOSE THE PLAYLIST{% endtrans %}</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="form-check">
                    {% if app.user %}
                        {% for playlist in playlists %}
                            <div>
                                <a class="neon white_hover" href="{{ path('add_song_homepage', {'songId': song.id, 'playlistId': playlist.id}) }}"> {{ playlist.name }} </a>
                            </div>
                        {% endfor %}
                    {% else %}
                        <div class="neon white_hover">{% trans %}Sorry ... you must be connected{% endtrans %}</div>
                        <div class="neon white_hover">{% trans %}Click here to sign in:{% endtrans %}
                            <a class="neon white_hover" href="{{ path('app_login') }}">{% trans %}SIGN IN{% endtrans %}</a>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
