{% if form  %}
    {{ form_start(form) }}
    <div class="input-group ">

        {{ form_widget(form.q, {'attr':{'class':''}}) }}

        <button type="submit" class="btn btn-outline-primary">search</button>

    </div>
    {{ form_end(form) }}
{% endif %}