{{ form_start(form) }}
<div class="input-group ">

    {{ form_widget(form.q, {'attr':{'class':'form-control rounded'}}) }}

    <button type="submit" class="btn btn-outline-primary">search</button>

</div>
{{ form_end(form) }}