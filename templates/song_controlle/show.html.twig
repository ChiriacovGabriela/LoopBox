{% extends 'base.html.twig' %}

{% block title %}Song{% endblock %}

{% block body %}
    <h1>Song</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ song.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ song.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ song.type }}</td>
            </tr>
            <tr>
                <th>Artist</th>
                <td>{{ song.artist }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ song.createdAt ? song.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Deleted_at</th>
                <td>{{ song.deletedAt ? song.deletedAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ song.updatedAt ? song.updatedAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PicturePath</th>
                <td>{{ song.picturePath }}</td>
            </tr>
            <tr>
                <th>AudioPath</th>
                <td>{{ song.audioPath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_song_controlle_index') }}">back to list</a>

    <a href="{{ path('app_song_controlle_edit', {'id': song.id}) }}">edit</a>

    {{ include('song_controlle/_delete_form.html.twig') }}
{% endblock %}
