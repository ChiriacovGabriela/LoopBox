{{ form_start(playlistForm) }}

    <div class="form-outline form-white mb-4">
    {{ form_widget(playlistForm.name) }}
    {{ form_label(playlistForm.name) }}
    </div>

    <div class="form-outline form-white mb-4">
    {{ form_widget(playlistForm.imagePath) }}
    <div>Image</div>
    </div>

    <div class="btn1"> <button class="btn btn-outline-light btn-lg px-5" type="submit">Create</button></div>
{{ form_end(playlistForm) }}