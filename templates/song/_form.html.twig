{{ form_start(form) }}

    {{ form_widget(form) }}
    {% if app.request.locale=="en" %}
        <button class="btn btn-light btn-lg px-5">{{ button_label|default('Save') }}</button>
    {% else %}
        <button class="btn btn-light btn-lg px-5">{{ button_label|default('Sauvegarder') }}</button>
    {% endif %}

{{ form_end(form) }}
